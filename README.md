# TCP_Server_thread_pool
Sencillo servidor hecho en python utilizando thread pool. El servidor recibe la frase y devuelve cuántas veces se presenta el último carácter, a su vez, en un archivo de texto van quedando registros con la fecha, frase recibida, número de veces que se repite, y si dicho número es primo.
